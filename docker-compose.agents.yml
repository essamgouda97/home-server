# Auto-generated — do not edit manually
# Run: make gen-compose

services:

  servo-gateway:
    image: moltbot:local
    container_name: servo-gateway
    env_file:
      - .env
      - agents/servo/.env
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
      - NODE_ENV=production
      - OPENCLAW_GATEWAY_BIND=127.0.0.1
      - OPENCLAW_GATEWAY_PORT=18789
      - OPENCLAW_STATE_DIR=/home/node/.openclaw
    volumes:
      - /mnt/server/agents/servo/config:/home/node/.openclaw:rw
      - ./agents/servo/workspace:/home/node/.openclaw/workspace:rw
      - /mnt/server/media:/media:ro
    ports:
      - "127.0.0.1:18789:18789"
      - "127.0.0.1:18790:18790"
      - "127.0.0.1:18793:18793"
    networks:
      - proxy
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:18789/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  servo-cli:
    image: moltbot:local
    container_name: servo-cli
    env_file:
      - .env
      - agents/servo/.env
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
      - OPENCLAW_STATE_DIR=/home/node/.clawdbot
    volumes:
      - /mnt/server/agents/servo/config:/home/node/.clawdbot:rw
      - ./agents/servo/workspace:/home/node/clawd:rw
    networks:
      - proxy
    entrypoint: ["node", "dist/index.js"]
    command: ["--help"]
    stdin_open: true
    tty: true
    profiles:
      - cli
