# Moltbot Gateway Token (generate with: openssl rand -hex 32)
MOLTBOT_GATEWAY_TOKEN=

# Authentication Method: Choose ONE of the following:

# Option 1: Claude Pro Account (Recommended - use your existing Claude.ai account)
# Leave ANTHROPIC_API_KEY empty and use: docker compose run --rm moltbot-cli auth login
# This will open a browser/show URL to login with your Claude Pro account

# Option 2: Anthropic API Key (for API access, costs money per token)
# Get from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=
