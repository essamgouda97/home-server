[Unit]
Description=Restrict Docker ingress to household LAN and Tailscale
After=docker.service network-online.target
Requires=docker.service
PartOf=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/home-server-docker-ingress
RemainAfterExit=yes

[Install]
WantedBy=docker.service
