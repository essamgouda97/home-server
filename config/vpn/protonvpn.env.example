# Copy privately to ~/.config/home-server/secrets/protonvpn.env (mode 600).
# Generate a Proton VPN WireGuard configuration with NAT-PMP enabled, then place
# only its PrivateKey value here. Never commit the populated file.
WIREGUARD_PRIVATE_KEY=

