groups:
  - name: home-server
    rules:
      - alert: DiskSpaceLow
        expr: home_filesystem_available_bytes / home_filesystem_size_bytes < 0.10
        for: 10m
        labels: {severity: warning}
        annotations: {summary: 'Less than 10% disk space remains on {{ $labels.mountpoint }}'}
      - alert: ContainerDown
        expr: home_container_running == 0
        for: 3m
        labels: {severity: critical}
        annotations: {summary: '{{ $labels.name }} is stopped'}
      - alert: ContainerUnhealthy
        expr: home_container_healthy == 0
        for: 3m
        labels: {severity: warning}
        annotations: {summary: '{{ $labels.name }} failed its health check'}
      - alert: ServiceUnavailable
        expr: home_service_up == 0
        for: 3m
        labels: {severity: critical}
        annotations: {summary: '{{ $labels.service }} is unavailable'}
      - alert: MetricsCollectorStale
        expr: time() - home_metrics_collected_seconds > 180
        for: 2m
        labels: {severity: warning}
        annotations: {summary: 'The host metrics collector stopped updating'}
      - alert: MemoryPressure
        expr: node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes < 0.10
        for: 10m
        labels: {severity: warning}
        annotations: {summary: 'Less than 10% host memory is available'}
      - alert: TorrentMetricsUnavailable
        expr: home_qbittorrent_metrics_up == 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: 'qBittorrent aggregate metrics cannot be collected'}
      - alert: TorrentSeedPolicyDrift
        expr: home_qbittorrent_seed_policy_ok == 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: 'Torrent ratio or completed-download retention settings changed'}
      - alert: TorrentDemandStalled
        expr: home_qbittorrent_with_demand > 0 and home_qbittorrent_uploading == 0
        for: 6h
        labels: {severity: warning}
        annotations: {summary: 'Torrent peers show demand but no upload has run for six hours'}
